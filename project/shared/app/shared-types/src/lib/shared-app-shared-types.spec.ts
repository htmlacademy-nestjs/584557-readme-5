import { sharedAppSharedTypes } from './shared-app-shared-types';

describe('sharedAppSharedTypes', () => {
  it('should work', () => {
    expect(sharedAppSharedTypes()).toEqual('shared-app-shared-types');
  });
});
